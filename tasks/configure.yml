- name: "mkdir confdir"
  file: dest=/etc/nginx/conf.d state=directory

- name: put upstart script
  template: src=init.conf.j2 dest=/etc/init/nginx.conf owner=root group=root mode=0644

- name: "edit config"
  template: src=nginx.conf.j2 dest=/etc/nginx/nginx.conf owner=root group=root mode=0644
